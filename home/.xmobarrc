   Config { font = "-misc-fixed-*-*-*-*-10-*-*-*-*-*-*-*"
       , bgColor = "black"
       , fgColor = "grey"
       , position = Top
       , lowerOnStart = True
       , commands = [ Run Date "%e[%H:%M]" "date" 60
       	 	    , Run Com "ncmpcpp --now-playing" [] "music" 10 
		    , Run Com "/home/lulz/scripts/cputemp.sh" [] "cpuTemp" 10  
		    , Run StdinReader
                    ]
       , sepChar = "%"
       , alignSep = "}{"
       , template = "%StdinReader% } %music% { <fc=grey>%date%</fc>"
      }
